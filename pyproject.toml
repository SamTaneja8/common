[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "common-utils"
version = "0.1.0"
description = "Shared utilities for sibling scraper repos"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
